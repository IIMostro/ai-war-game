[project]
name = "ai-war-game"
version = "0.1.0"
description = "AI-driven sandbox warfare game built on Hermes Agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pip-audit>=2.10.0",
    "pytest>=9.0.3",
    "ruff>=0.15.11",
]
